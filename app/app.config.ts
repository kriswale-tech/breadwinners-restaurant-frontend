export default defineAppConfig({
  ui: {
    // Warm, bakery/restaurant-friendly accent color used across Nuxt UI components.
    // This will be dynamically overridden by the theme system based on page meta
    primary: 'amber' as any
  } as any
})

